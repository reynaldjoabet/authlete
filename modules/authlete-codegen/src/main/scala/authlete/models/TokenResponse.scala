/**
 * Authlete API
 * Welcome to the **Authlete API documentation**. Authlete is an **API-first service** where every aspect of the  platform is configurable via API. This documentation will help you authenticate and integrate with Authlete to  build powerful OAuth 2.0 and OpenID Connect servers.  At a high level, the Authlete API is grouped into two categories:  - **Management APIs**: Enable you to manage services and clients. - **Runtime APIs**: Allow you to build your own Authorization Servers or Verifiable Credential (VC) issuers.  ## 🌐 API Servers  Authlete is a global service with clusters available in multiple regions across the world:  - 🇺🇸 **US**: `https://us.authlete.com` - 🇯🇵 **Japan**: `https://jp.authlete.com` - 🇪🇺 **Europe**: `https://eu.authlete.com` - 🇧🇷 **Brazil**: `https://br.authlete.com`  Our customers can host their data in the region that best meets their requirements.  ## 🔑 Authentication  All API endpoints are secured using **Bearer token authentication**. You must include an access token in every request:  ``` Authorization: Bearer YOUR_ACCESS_TOKEN ```  ### Getting Your Access Token  Authlete supports two types of access tokens:  **Service Access Token** - Scoped to a single service (authorization server instance)  1. Log in to [Authlete Console](https://console.authlete.com) 2. Navigate to your service → **Settings** → **Access Tokens** 3. Click **Create Token** and select permissions (e.g., `service.read`, `client.write`) 4. Copy the generated token  **Organization Token** - Scoped to your entire organization  1. Log in to [Authlete Console](https://console.authlete.com) 2. Navigate to **Organization Settings** → **Access Tokens** 3. Click **Create Token** and select org-level permissions 4. Copy the generated token  > ⚠️ **Important Note**: Tokens inherit the permissions of the account that creates them. Service tokens can only  > access their specific service, while organization tokens can access all services within your org.  ### Token Security Best Practices  - **Never commit tokens to version control** - Store in environment variables or secure secret managers - **Rotate regularly** - Generate new tokens periodically and revoke old ones - **Scope appropriately** - Request only the permissions your application needs - **Revoke unused tokens** - Delete tokens you're no longer using from the console  ### Quick Test  Verify your token works with a simple API call:  ```bash curl -X GET https://us.authlete.com/api/service/get/list \\   -H \"Authorization: Bearer YOUR_ACCESS_TOKEN\" ```  ## 🎓 Tutorials  If you're new to Authlete or want to see sample implementations, these resources will help you get started:  - [Getting Started with Authlete](https://www.authlete.com/developers/getting_started/) - [From Sign-Up to the First API Request](https://www.authlete.com/developers/tutorial/signup/)  ## 🛠 Contact Us  If you have any questions or need assistance, our team is here to help:  - [Contact Page](https://www.authlete.com/contact/) 
 *
 * The version of the OpenAPI document: 3.0.16
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package authlete.models

import authlete.models.TokenType.*
import java.net.URI
import com.github.plokhotnyuk.jsoniter_scala.macros.named

case class TokenResponse(
  /* The code which represents the result of the API call. */
  @named("resultCode") resultCode: Option[String] = scala.None,
  /* A short message which explains the result of the API call. */
  @named("resultMessage") resultMessage: Option[String] = scala.None,
  /* The next action that the authorization server implementation should take. */
  @named("action") action: Option[TokenResponseEnums.Action] = scala.None,
  /* The content that the authorization server implementation is to return to the client application. Its format varies depending on the value of `action` parameter.  */
  @named("responseContent") responseContent: Option[String] = scala.None,
  /* The value of `username` request parameter in the token request. The client application must specify username when it uses [Resource Owner Password Grant](https://datatracker.ietf.org/doc/html/rfc6749#section-4.3). In other words, when the value of `grant_type` request parameter is `password`, `username` request parameter must come along.  This parameter has a value only if the value of `grant_type` request parameter is `password` and the token request is valid.  */
  @named("username") username: Option[String] = scala.None,
  /* The value of `password` request parameter in the token request. The client application must specify password when it uses [Resource Owner Password Grant](https://datatracker.ietf.org/doc/html/rfc6749#section-4.3). In other words, when the value of `grant_type` request parameter is `password`, `password` request parameter must come along.  This parameter has a value only if the value of `grant_type` request parameter is `password` and the token request is valid.  */
  @named("password") password: Option[String] = scala.None,
  /* The ticket which is necessary to call Authlete's `/auth/token/fail` API or `/auth/token/issue` API.  This parameter has a value only if the value of `grant_type` request parameter is `password` and the token request is valid.  */
  @named("ticket") ticket: Option[String] = scala.None,
  /* The newly issued access token. */
  @named("accessToken") accessToken: Option[String] = scala.None,
  /* The datetime at which the newly issued access token will expire. The value is represented in milliseconds since the Unix epoch (1970-01-01).  */
  @named("accessTokenExpiresAt") accessTokenExpiresAt: Option[Long] = scala.None,
  /* The duration of the newly issued access token in seconds. */
  @named("accessTokenDuration") accessTokenDuration: Option[Long] = scala.None,
  /* The newly issued refresh token. */
  @named("refreshToken") refreshToken: Option[String] = scala.None,
  /* The datetime at which the newly issued refresh token will expire. The value is represented in milliseconds since the Unix epoch (1970-01-01).  */
  @named("refreshTokenExpiresAt") refreshTokenExpiresAt: Option[Long] = scala.None,
  /* The duration of the newly issued refresh token in seconds. */
  @named("refreshTokenDuration") refreshTokenDuration: Option[Long] = scala.None,
  /* The newly issued ID token. Note that an ID token is issued from a token endpoint only when the `response_type` request parameter of the authorization request to an authorization endpoint has contained `code` and the `scope` request parameter has contained `openid`.  */
  @named("idToken") idToken: Option[String] = scala.None,
  /* The grant type of the token request. */
  @named("grantType") grantType: Option[String] = scala.None,
  /* The client ID. */
  @named("clientId") clientId: Option[Long] = scala.None,
  /* The client ID alias when the token request was made. If the client did not have an alias, this parameter is `null`. Also, if the token request was invalid and it failed to identify a client, this parameter is `null`.  */
  @named("clientIdAlias") clientIdAlias: Option[String] = scala.None,
  /* The flag which indicates whether the client ID alias was used when the token request was made. `true` if the client ID alias was used when the token request was made.  */
  @named("clientIdAliasUsed") clientIdAliasUsed: Option[Boolean] = scala.None,
  /* The subject (= resource owner's ID) of the access token. Even if an access token has been issued by the call of `/api/auth/token` API, this parameter is `null` if the flow of the token request was [Client Credentials Flow](https://datatracker.ietf.org/doc/html/rfc6749#section-4.4) (`grant_type=client_credentials`) because it means the access token is not associated with any specific end-user.  */
  @named("subject") subject: Option[String] = scala.None,
  /* The scopes covered by the access token. */
  @named("scopes") scopes: Option[Seq[String]] = scala.None,
  /* The extra properties associated with the access token. This parameter is `null` when no extra property is associated with the issued access token.  */
  @named("properties") properties: Option[Seq[Property]] = scala.None,
  /* The newly issued access token in JWT format. If the authorization server is configured to issue JWT-based access tokens (= if the service's `accessTokenSignAlg` value is a non-null value), a JWT-based access token is issued along with the original random-string one.  */
  @named("jwtAccessToken") jwtAccessToken: Option[String] = scala.None,
  /* The resources specified by the `resource` request parameters in the token request. See \"Resource Indicators for OAuth 2.0\" for details.  */
  @named("resources") resources: Option[Seq[String]] = scala.None,
  /* The target resources of the access token being issued. See \"Resource Indicators for OAuth 2.0\" for details.  */
  @named("accessTokenResources") accessTokenResources: Option[Seq[String]] = scala.None,
  @named("authorizationDetails") authorizationDetails: Option[AuthzDetails] = scala.None,
  /* Additional claims to be embedded in an ID token.  */
  @named("additionalClaims") additionalClaims: Option[String] = scala.None,
  /* The attributes of this service that the client application belongs to.  */
  @named("serviceAttributes") serviceAttributes: Option[Seq[Pair]] = scala.None,
  /* The attributes of the client.  */
  @named("clientAttributes") clientAttributes: Option[Seq[Pair]] = scala.None,
  /* The client authentication method that was performed at the token endpoint.  */
  @named("clientAuthMethod") clientAuthMethod: Option[String] = scala.None,
  /* the value of the `grant_id` request parameter of the device authorization request.  The `grant_id` request parameter is defined in [Grant Management for OAuth 2.0](https://openid.net/specs/fapi-grant-management.html) , which is supported by Authlete 2.3 and newer versions.  */
  @named("grantId") grantId: Option[String] = scala.None,
  /* The audiences on the token exchange request  */
  @named("audiences") audiences: Option[Seq[String]] = scala.None,
  @named("requestedTokenType") requestedTokenType: Option[TokenType] = scala.None,
  @named("subjectToken") subjectToken: Option[String] = scala.None,
  @named("subjectTokenType") subjectTokenType: Option[TokenType] = scala.None,
  @named("subjectTokenInfo") subjectTokenInfo: Option[TokenInfo] = scala.None,
  @named("actorToken") actorToken: Option[String] = scala.None,
  @named("actorTokenType") actorTokenType: Option[TokenType] = scala.None,
  @named("actorTokenInfo") actorTokenInfo: Option[TokenInfo] = scala.None,
  /* For RFC 7523 JSON Web Token (JWT) Profile for OAuth 2.0 Client Authentication and Authorization Grants  */
  @named("assertion") assertion: Option[String] = scala.None,
  /* Indicate whether the previous refresh token that had been kept in the database for a short time was used  */
  @named("previousRefreshTokenUsed") previousRefreshTokenUsed: Option[Boolean] = scala.None,
  /* The entity ID of the client.  */
  @named("clientEntityId") clientEntityId: Option[String] = scala.None,
  /* Flag which indicates whether the entity ID of the client was used when the request for the access token was made.  */
  @named("clientEntityIdUsed") clientEntityIdUsed: Option[Boolean] = scala.None,
  /* Duration of the `c_nonce` in seconds.  */
  @named("cnonceDuration") cnonceDuration: Option[Long] = scala.None,
  /* Get the expected nonce value for DPoP proof JWT, which should be used as the value of the `DPoP-Nonce` HTTP header.  */
  @named("dpopNonce") dpopNonce: Option[String] = scala.None,
  /* Get the `c_nonce`.  */
  @named("cnonce") cnonce: Option[String] = scala.None,
  /* Get the time at which the `c_nonce` expires in milliseconds since the Unix epoch (1970-01-01).  */
  @named("cnonceExpiresAt") cnonceExpiresAt: Option[Long] = scala.None,
  /* Get the names of the claims that the authorization request (which resulted in generation of the access token) requested to be embedded in ID tokens.  */
  @named("requestedIdTokenClaims") requestedIdTokenClaims: Option[Seq[String]] = scala.None,
  /* Scopes associated with the refresh token.  */
  @named("refreshTokenScopes") refreshTokenScopes: Option[Seq[String]] = scala.None,
  /* The session ID, which is the ID of the user's authentication session, associated with a newly created access token.  */
  @named("sessionId") sessionId: Option[String] = scala.None,
  /* If the response from the `/auth/token` API contains the `deviceSecret` parameter, its value should be used as the value of this `deviceSecret` request parameter to the `/nativesso` API. The authorization server may choose to issue a new device secret; in that case, it is free to generate a new device secret and specify the new value.  */
  @named("deviceSecret") deviceSecret: Option[String] = scala.None,
  /* The authorization server should compute the hash value of the device secret based on its own logic and specify the computed hash as the value of this `deviceSecretHash` request parameter to the `/nativesso` API.  */
  @named("deviceSecretHash") deviceSecretHash: Option[String] = scala.None,
  /* The location of the client's metadata document that was used to resolve client metadata.  This property is set when client metadata was retrieved via the [OAuth Client ID Metadata Document](https://datatracker.ietf.org/doc/draft-ietf-oauth-client-id-metadata-document/) (CIMD) mechanism.  */
  @named("metadataDocumentLocation") metadataDocumentLocation: Option[URI] = scala.None,
  /* Flag indicating whether a metadata document was used to resolve client metadata for this request.  When `true`, the client metadata was retrieved via the CIMD mechanism rather than from the Authlete database.  */
  @named("metadataDocumentUsed") metadataDocumentUsed: Option[Boolean] = scala.None
)

object TokenResponseEnums:
  enum Action:
    case `INTERNAL_SERVER_ERROR`
    case `INVALID_CLIENT`
    case `BAD_REQUEST`
    case `PASSWORD`
    case `OK`
    case `TOKEN_EXCHANGE`
    case `JWT_BEARER`
    case `NATIVE_SSO`
    case `ID_TOKEN_REISSUABLE`

  object Action:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given actionCodec: JsonValueCodec[Action] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "INTERNAL_SERVER_ERROR" => "INTERNAL_SERVER_ERROR"
            case "INVALID_CLIENT" => "INVALID_CLIENT"
            case "BAD_REQUEST" => "BAD_REQUEST"
            case "PASSWORD" => "PASSWORD"
            case "OK" => "OK"
            case "TOKEN_EXCHANGE" => "TOKEN_EXCHANGE"
            case "JWT_BEARER" => "JWT_BEARER"
            case "NATIVE_SSO" => "NATIVE_SSO"
            case "ID_TOKEN_REISSUABLE" => "ID_TOKEN_REISSUABLE"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end TokenResponseEnums
