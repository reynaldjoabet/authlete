/**
 * Authlete API
 * Welcome to the **Authlete API documentation**. Authlete is an **API-first service** where every aspect of the  platform is configurable via API. This documentation will help you authenticate and integrate with Authlete to  build powerful OAuth 2.0 and OpenID Connect servers.  At a high level, the Authlete API is grouped into two categories:  - **Management APIs**: Enable you to manage services and clients. - **Runtime APIs**: Allow you to build your own Authorization Servers or Verifiable Credential (VC) issuers.  ## 🌐 API Servers  Authlete is a global service with clusters available in multiple regions across the world:  - 🇺🇸 **US**: `https://us.authlete.com` - 🇯🇵 **Japan**: `https://jp.authlete.com` - 🇪🇺 **Europe**: `https://eu.authlete.com` - 🇧🇷 **Brazil**: `https://br.authlete.com`  Our customers can host their data in the region that best meets their requirements.  ## 🔑 Authentication  All API endpoints are secured using **Bearer token authentication**. You must include an access token in every request:  ``` Authorization: Bearer YOUR_ACCESS_TOKEN ```  ### Getting Your Access Token  Authlete supports two types of access tokens:  **Service Access Token** - Scoped to a single service (authorization server instance)  1. Log in to [Authlete Console](https://console.authlete.com) 2. Navigate to your service → **Settings** → **Access Tokens** 3. Click **Create Token** and select permissions (e.g., `service.read`, `client.write`) 4. Copy the generated token  **Organization Token** - Scoped to your entire organization  1. Log in to [Authlete Console](https://console.authlete.com) 2. Navigate to **Organization Settings** → **Access Tokens** 3. Click **Create Token** and select org-level permissions 4. Copy the generated token  > ⚠️ **Important Note**: Tokens inherit the permissions of the account that creates them. Service tokens can only  > access their specific service, while organization tokens can access all services within your org.  ### Token Security Best Practices  - **Never commit tokens to version control** - Store in environment variables or secure secret managers - **Rotate regularly** - Generate new tokens periodically and revoke old ones - **Scope appropriately** - Request only the permissions your application needs - **Revoke unused tokens** - Delete tokens you're no longer using from the console  ### Quick Test  Verify your token works with a simple API call:  ```bash curl -X GET https://us.authlete.com/api/service/get/list \\   -H \"Authorization: Bearer YOUR_ACCESS_TOKEN\" ```  ## 🎓 Tutorials  If you're new to Authlete or want to see sample implementations, these resources will help you get started:  - [Getting Started with Authlete](https://www.authlete.com/developers/getting_started/) - [From Sign-Up to the First API Request](https://www.authlete.com/developers/tutorial/signup/)  ## 🛠 Contact Us  If you have any questions or need assistance, our team is here to help:  - [Contact Page](https://www.authlete.com/contact/) 
 *
 * The version of the OpenAPI document: 3.0.16
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package authlete.models

import com.github.plokhotnyuk.jsoniter_scala.macros.named

case class AuthorizationIssueRequest(
  /* The ticket issued from Authlete `/auth/authorization` API.  */
  @named("ticket") ticket: String,
  /* The subject (= a user account managed by the service) who has granted authorization to the client application.  */
  @named("subject") subject: String,
  /* The time when the authentication of the end-user occurred. Its value is the number of seconds from `1970-01-01`.  */
  @named("authTime") authTime: Option[Long] = scala.None,
  /* The Authentication Context Class Reference performed for the end-user authentication. */
  @named("acr") acr: Option[String] = scala.None,
  /* The claims of the end-user (= pieces of information about the end-user) in JSON format. See [OpenID Connect Core 1.0, 5.1. Standard Claims](https://openid.net/specs/openid-connect-core-1_0.html#StandardClaims) for details about the format.  */
  @named("claims") claims: Option[String] = scala.None,
  /* Extra properties to associate with an access token and/or an authorization code. */
  @named("properties") properties: Option[Seq[Property]] = scala.None,
  /* Scopes to associate with an access token and/or an authorization code. If a non-empty string array is given, it replaces the scopes specified by the original authorization request.  */
  @named("scopes") scopes: Option[Seq[String]] = scala.None,
  /* The value of the `sub` claim to embed in an ID token. If this request parameter is `null` or empty, the value of the `subject` request parameter is used as the value of the `sub` claim.  */
  @named("sub") sub: Option[String] = scala.None,
  /* JSON that represents additional JWS header parameters for ID tokens that may be issued based on the authorization request.  */
  @named("idtHeaderParams") idtHeaderParams: Option[String] = scala.None,
  /* Claim key-value pairs that are used to compute transformed claims.  */
  @named("claimsForTx") claimsForTx: Option[String] = scala.None,
  /* the claims that the user has consented for the client application to know.  */
  @named("consentedClaims") consentedClaims: Option[Seq[String]] = scala.None,
  @named("authorizationDetails") authorizationDetails: Option[AuthzDetails] = scala.None,
  /* Additional claims that are added to the payload part of the JWT access token.  */
  @named("jwtAtClaims") jwtAtClaims: Option[String] = scala.None,
  /* The representation of an access token that may be issued as a result of the Authlete API call.  */
  @named("accessToken") accessToken: Option[String] = scala.None,
  /* The duration (in seconds) of the access token that may be issued as a result of the Authlete API call.  When this request parameter holds a positive integer, it is used as the duration of the access token in. In other cases, this request parameter is ignored.  */
  @named("accessTokenDuration") accessTokenDuration: Option[Long] = scala.None,
  /* The session ID of the user's authentication session. The specified value will be embedded in the ID token as the value of the `sid` claim. This parameter needs to be provided only if you want to support the [OpenID Connect Native SSO for Mobile Apps 1.0](https://openid.net/specs/openid-connect-native-sso-1_0.html) specification (a.k.a. \"Native SSO\"). To enable support for the Native SSO specification, the `nativeSsoSupported` property of your service must be set to `true`.  */
  @named("sessionId") sessionId: Option[String] = scala.None,
  /* The type of the `aud` claim of the ID token being issued. Valid values are as follows.  | Value | Description | | ----- | ----------- | | \"array\" | The type of the aud claim is always an array of strings. | | \"string\" | The type of the aud claim is always a single string. | | null | The type of the aud claim remains the same as before. |  This request parameter takes precedence over the `idTokenAudType` property of the service.  */
  @named("idTokenAudType") idTokenAudType: Option[String] = scala.None,
  /* Values of verified claims requested indirectly by \"transformed claims\".  */
  @named("verifiedClaimsForTx") verifiedClaimsForTx: Option[Seq[String]] = scala.None
)

